---
title: MCP + CLI
description: Conecta tu agente al corpus, o úsalo desde la terminal.
---

El paquete [`@crafter/amicus`](https://github.com/crafter-research/legalize-pe-engine/tree/main/apps/amicus)
es un cliente liviano sobre la API hosted (sin clonar el corpus). Da un servidor
MCP y un CLI que comparten el mismo core.

## CLI

```bash
npx @crafter/amicus search "prescripción adquisitiva" --jurisdiction pe
npx @crafter/amicus get pe/RM-0702-2026-IN
npx @crafter/amicus ask "¿qué dice el Código Civil sobre la prescripción adquisitiva?"
npx @crafter/amicus jurisdictions
```

Agrega `--json` para salida de máquina. `ask` requiere `AMICUS_API_KEY`.

## MCP server

Tools: `search_norms`, `get_norm`, `ask_legal` (grounded + cita),
`list_jurisdictions`.

### Claude Desktop / Cursor / Codex

```json
{
  "mcpServers": {
    "amicus": {
      "command": "npx",
      "args": ["-y", "@crafter/amicus", "mcp"],
      "env": { "AMICUS_API_KEY": "amicus_sk_..." }
    }
  }
}
```

`search_norms` y `get_norm` funcionan sin key; `ask_legal` necesita
`AMICUS_API_KEY`.

## Hosted vs corpus local

| | `@crafter/amicus` (hosted) | `@legalize-pe/mcp` (corpus) |
| --- | --- | --- |
| Búsqueda | Híbrida (semántica + FTS) | Keyword |
| `ask` grounded | Sí | No |
| Clona el corpus | No | Sí (~600MB) |
| `get_norm_history` (git) | No | Sí |

Usa el **hosted** para preguntar y buscar con calidad; el **corpus** para
self-host y el historial git de cada reforma.
