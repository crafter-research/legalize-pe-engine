.vercel

# Astro content-layer cache: regenerated by `astro check`/dev/build every run,
# so it churns the diff each session. The tracked .astro/*.d.ts type files stay.
.astro/data-store.json
.astro/settings.json
